package Learn;

import java.util.Scanner;

public class PasswordCheck {
	
	public static void main(String[] args) {
		
		//String pass = "ram1234one";
		
		int numcount =0;
		int charcount =0;
		
		System.out.println(" enter a password");
		
		String input = new Scanner(System.in).nextLine();
		
					
		
			
			int p = 0;
			if (input.charAt(p)>='A' && input.charAt(p)<='Z') {
				
				;
			}
			else if (input.charAt(p)>='A' && input.charAt(p)<='Z') {
				if(Character.isLetter(input.charAt(p)));
				p++;
	}
		//for(digit < input.length; );
	}   //char p = input.charAt(0);
		//int digit =0;
		
}
	
